[Unit]
Description=Kilnr CI isolated Docker network
Requires=docker.service
After=docker.service
Before=kilnr-controller.service
PartOf=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/libexec/kilnr/network-setup
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
